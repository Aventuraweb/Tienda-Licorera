---
//  formLogin: Sección formulario para el login 
const { label, id, type, placeholder } = Astro.props;
---

<div class="mb-6">
    <label for={id} class="block text-sm font-bold mb-2">{label}</label>
    <input 
        type={type} 
        id={id} 
        placeholder={placeholder}   
        class="w-full p-2 border-2 border-blue-400 rounded bg-transparent focus:outline-none focus:ring-2 focus:ring-blue-500"  />
</div>
