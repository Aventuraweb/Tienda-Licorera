---
//  formLogin: Sección formulario para el login 
const { label, id, type, placeholder } = Astro.props;
---

<div class="mb-6">
    <label for={id} class="block text-sm font-semibold mb-2">{label}</label>
    <input type={type} id={id} class="w-full p-3 rounded bg-transparent border-2 border-blue-400 text-white placeholder-gray-400 focus:outline-none focus:border-blue-300" placeholder={placeholder} />
</div>
